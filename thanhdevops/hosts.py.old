from django_hosts import patterns, host

host_patterns = patterns(
    '',
    host(r'www\.thanhdevops\.info', 'landing.urls', name='default'),
    host(r'catshelter\.thanhdevops\.info', 'catshelter.urls', name='catshelter'),
)
